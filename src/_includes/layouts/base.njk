<!DOCTYPE html>
<html lang="en" class="text-gray-500 dark:bg-gray-800 dark:text-gray-100">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {% if description %}
      <meta name="description" content="{{description}}" />
    {% endif %}
    <title>{{ title }} | Wilson Parson</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body class="w- mx-auto p-4">
    <header class="flex items-center justify-between">
      <a href="/" class="w-16 md:width-20">
        {% include "logo.html" %}
      </a>
      {% include "nav.njk" %}
    </header>
    <main class="markdown">
      <h1>{{ title }}</h1>
      <p>{{ description }}</p>
      {{ content | safe }}
    </main>
  </body>
</html>
