<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {% if description %}
      <meta name="description" content="{{description}}" />
    {% endif %}
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body class="dark:bg-gray-800 dark:text-gray-100 p-4">
    <header>
      <div class="w-20">
        {% include "logo.html" %}
      </div>
    </header>
    <main class="markdown">
      {{ content | safe }}
    </main>
  </body>
</html>
